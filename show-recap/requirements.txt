# Show Recap Service Dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Audio processing (uses ffmpeg via subprocess)
# ffmpeg must be installed on the system

# Music recognition (detect commercial break music)
pyacrcloud>=1.0.0

# Cloud storage
dropbox>=12.0.0

# SMS notifications
twilio>=9.0.0

# File system watching (for USB detection)
watchdog>=4.0.0
