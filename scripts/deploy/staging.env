# Canonical Band Tools staging (Fly.io) configuration.
# Sourced by scripts/deploy/staging.sh — do not run directly.

DEPLOY_ENV=staging
FLY_APP_NAME=bandtools-test
FLY_CONFIG=fly.test.toml
FLY_VOLUME_NAME=bandtools_test_data
FLY_REGION=ord
STAGING_URL=https://bandtools-test.fly.dev
